Mark’s Version is Flask based (local host)
Based on http://flask.pocoo.org/docs/dev/tutorial/database/
Cd into the directory of myproject
Run these commands in terminal (MAC users)


. venv/bin/activate
flask init-db


export FLASK_APP=flaskr
export FLASK_ENV=development
flask run


Problem: This doesn’t upload to Cloud storage as the syntax is different


Windows Command Line:
Cd into myproject
py -3 -m venv venv


venv\Scripts\activate


Py -m pip install 
        Flask, seaborn, numpy, pandas, maplotlib, sklearn


Set FLASK_APP=flaskr
Set FLASK_ENV=development
Flask run